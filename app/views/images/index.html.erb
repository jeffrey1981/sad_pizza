<h1>SAD PIZZA</h1>

<section class="pizza-feed">
  <% @posts.each do |post| %>
    <%= link_to post_path(post) do %>
      <%= image_tag post.image, class: "pizza-pic" %>
      <p><%= post.story %></p>
    <% end %>
  <% end %>
</section>

